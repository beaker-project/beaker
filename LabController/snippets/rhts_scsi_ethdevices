## Only load the following devices if $devices is set
## scsidevices=qla2xxx,megaraid_mbox
## ethdevices=tg3,e1000
#if $getVar('scsidevices', '') != ''
#set $scsidevices = ':'.join($getVar('scsidevices','').split(','))
device scsi $scsidevices
#end if
#if $getVar('ethdevices', '') != ''
#set $ethdevices = ':'.join($getVar('ethdevices','').split(','))
device eth $ethdevices
#end if
