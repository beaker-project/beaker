Define primary lab controller (bug 717424)
------------------------------------------

Add the column:

    ALTER TABLE lab_controller
    ADD COLUMN primary_mirror BOOLEAN NOT NULL DEFAULT 0;

To roll back:

    ALTER TABLE lab_controller
    DROP COLUMN primary_mirror;
