This directory contains generated asset files. These *should* be built as part 
of Beaker's build but currently they are built by hand and committed, in order 
to avoid a build dependency on the nodejs stack.
