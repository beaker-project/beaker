{% if user and user.sshpubkeys %}
mkdir -p /root/.ssh
cat >>/root/.ssh/authorized_keys <<"__EOF__"
{% for ssh_key in user.sshpubkeys %}
{{ ssh_key|string|replace('__EOF__', '') }}
{% endfor %}
__EOF__
restorecon -R /root/.ssh
chmod go-w /root /root/.ssh /root/.ssh/authorized_keys
{% endif %}
