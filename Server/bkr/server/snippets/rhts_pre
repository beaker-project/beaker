# Check in with Beaker Server
python << _EOF_
import xmlrpclib
server = xmlrpclib.Server("http://{{ system.lab_controller.fqdn }}:8000")
server.install_start("{{ system.fqdn }}")
_EOF_

{% if distro_tree is arch('ppc', 'ppc64') and (distro is osmajor('RedHatEnterpriseLinux7') or distro is osmajor('Fedora17')) %}
if command -v nvram >/dev/null ; then
    nvram --print-config=boot-device > /tmp/boot-device.bak
else
    logger "Error: nvram command not found!"
fi
{% endif %}
{#
## Enable installation monitoring
#}
{{ snippet('pre_anamon') }}
