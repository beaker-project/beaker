<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "beaker_user_guide.ent">
%BOOK_ENTITIES;
]>
<section id="chap-beaker_user_guide-Components-Recipes">
	<title>Recipes</title>
	<para>
		Recipes are contained within a Job (although indirectly, as directly they are contained in a <literal>Recipe Set</literal>) and are themselves a container for Tasks. There can be more than one Recipe per Job. The purpose of a Recipe is to group a set of Tasks into a single logical unit.
	</para>
	<section id="sect-beaker_user_guide-Components-Recipes-Recipe_Searching">
		<title>Recipe Searching</title>
		<para>
			The Recipe search is accessed through the <literal>Scheduler</literal> at the top of the page, and clicking on the <literal>Recipe</literal> link.
		</para>
		<mediaobject>
			<imageobject>
				<imagedata fileref="./images/recipe_search.png" format="PNG" width="444" />
			</imageobject>
		</mediaobject>
		<para>
			To look up the <literal>Recipe ID</literal> enter a number into the search box and press the <literal>Lookup ID</literal> button. See <xref linkend="sect-beaker_user_guide-Components-Systems-System_Searching"/> for details on searching.
			<note>
				<title>Quick Searches</title>
				<para>
					By pressing the <literal>Running</literal>,<literal>Queued</literal>, or <literal>Completed</literal> buttons you can quickly display Recipes that have a status of running,queued, and completed respectively.
				</para>
			</note>
		</para>
	</section>
	
	<section id="sect-beaker_user_guide-Components-Recipes-Recipe_Actions">
		<title>Recipe Actions</title>
		<para>
			At any time you may wish to cancel the Recipe, you may press the <literal>Cancel</literal> link that is placed under the <literal>Action</literal> column.
		</para>
	</section>

</section>

