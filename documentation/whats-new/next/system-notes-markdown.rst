System notes are rendered using Markdown
========================================

System notes are now rendered as HTML using Markdown, rather than displayed in 
<pre/>. That means notes can now take advantage of Markdown syntax for 
formatting, including hyperlinks.
