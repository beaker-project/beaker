from setuptools import setup, find_packages

setup(
    name = "bkr",
    version = "@VERSION@",

    packages=find_packages('.'),
    package_dir = {'':'.'},
    package_data={'': ['schema/*.rng', 'schema/*.ttl']},
    entry_points =  { 
        'console_scripts': (
            'beaker-stub-cobbler = bkr.common.stub_cobbler_load:main'
        ),
    }
)
