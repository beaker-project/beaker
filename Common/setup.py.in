from setuptools import setup, find_packages

setup(
    name = "bkr",
    version = "@VERSION@",

    packages=find_packages('.'),
    package_dir = {'':'.'},
    data_files = [('/etc/beaker/', ['message_bus.conf'])],
    package_data={'bkr.common': ['schema/*.rng', 'schema/*.ttl']},
    )
