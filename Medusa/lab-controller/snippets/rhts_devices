## Only load the following devices if $devices is set
## scsidevices=qla2xxx,megaraid_mbox
## ethdevices=tg3,e1000
#if $getVar('scsidevices', '') != ''
#set _devices = $getVar('scsidevices','').split(',')
#for $device in $_devices:
device $device
#end for
#end if
#if $getVar('ethdevices', '') != ''
#set _devices = $getVar('ethdevices','').split(',')
#for $device in $_devices:
device $device
#end for
#end if
